CD changer emulator offered as an option for Fiat Stilo, currently the emulator only works on the Connect NAV + navigator
(tested with firmware 8.3, 9.8 and 11.0)


Instructions:

Replace (rename the old one in .bak) the SoftwareSerial.cpp file in ... arduino \ libraries \ SoftwareSerial

Replace (rename the old in .bak) the wiring_pulse.c file in ... arduino \ hardware \ arduino \ cores \ arduino

Open the sketch cdc.pde and load in the Arduino board


Install the emulator on the Connect.
Turn on the Connect, after a few moments the LED "L" of Arduino lights up if it is recognized by the Connect.

Wait for the loading, the CDC writing should be black, press CDC and when the CDC SCAN disappears press Play, listen well.
